# INSPIRE 数据库提取平台操作指南

> **适用平台**：ZSTATS · INSPIRE 围手术期数据库提取平台
> **平台地址**：<https://zs.medsta.cn/Inspire_encryption/Inspire_data/inspire_data_1.0.2/>
> **数据版本**：INSPIRE 1.0.2（平台入口标记）/ INSPIRE v1.0（PhysioNet 官方版本号）
> **编写日期**：2026-09-13
> **编写方式**：以真实科研流程（术前白蛋白 → 术后院内死亡）完整跑通平台五步流程，所有界面文案、控件、列名、行数均为实机观测记录

---

## 目录

- [第一章 平台架构分析](#第一章-平台架构分析)
- [第二章 INSPIRE 数据的核心设计：分阶段](#第二章-inspire-数据的核心设计分阶段)
- [第三章 时间窗：同一变量的多次测量该取哪一次](#第三章-时间窗同一变量的多次测量该取哪一次)
- [第四章 变量字典与队列设计规则](#第四章-变量字典与队列设计规则)
- [第五章 功能逐项操作说明](#第五章-功能逐项操作说明)
- [第六章 完整提取流程示例（含实测结果）](#第六章-完整提取流程示例含实测结果)
- [第七章 注意事项与避坑清单](#第七章-注意事项与避坑清单)
- [附录 A 五阶段定义速查](#附录-a-五阶段定义速查)
- [附录 B 输出字段命名规则](#附录-b-输出字段命名规则)
- [附录 C 平台内置文案原文](#附录-c-平台内置文案原文)
- [附录 D 术语表](#附录-d-术语表)

---

## 第一章 平台架构分析

### 1.1 平台定位

本平台不是"数据下载站"，而是**队列设计与数据整理平台**。它把 INSPIRE 原始长表（约 2.7 亿行记录）预先整理成一张**以手术（`op_id`）为行、以"变量×时间阶段"为列的分析宽表**，用户需要做的只是"选指标 → 定时间窗 → 排人群 → 下载"。

平台首页原文写着：

> 该平台是数据整理与分析平台，如果你需要原始数据，请到 Inspire 官网下载：<https://physionet.org/content/inspire/1.0/>

也就是说：**原始数据去 PhysioNet 拿；要做研究队列，用这个平台。**

### 1.2 技术架构

| 层次 | 实现 |
|---|---|
| 后端计算 | **R / Shiny**（页面 `shiny-server-client`、`jquery-3.6.0`、`bootstrap-3.4.1`、`reactable`、`DT` 特征明显） |
| 前端组件 | `shinyWidgets`（virtual-select、sweetalert2）、`reactable`（变量列表）、`DataTables`（数据集预览）、`selectize`（下拉搜索）、`viz.js + grViz`（纳排流程图绘制） |
| 布局 | 单页多面板（Shiny `tabsetPanel` + `bsCollapse`），5 个步骤全部挂在同一个页面里，靠面包屑切换 |
| 数据规模 | 变量字典 **9,793 行**（全人群版）；最终数据集 **130,946 行**（每行一台手术） |
| 鉴权 | 独立脚本 `https://www.zstats.net/auhc.js`，见 1.3 |

### 1.3 登录与鉴权链路（实测还原）

平台部署在 `zs.medsta.cn`（跨域工具站），而账号体系在 `www.zstats.cn`（门户）。其鉴权脚本 `auhc.js` 的逻辑是：

```
1) URL 上带 ?st=st_xxxx 一次性票据（60 秒有效）
   → 调 https://www.zstats.cn/api/v1/sta/exchange?ticket=... 换取真 token
   → 写入 localStorage.userToken，并从地址栏抹掉 st 参数
2) 已有 token → 调 /api/v1/sta/authenticate?href=<当前路径>，请求头带 Ba-User-Token
3) 返回 code=302 → 弹"需要登录"→ 跳 www.zstats.cn/#/user/login?to=<回跳地址>
   返回 code=303 → 弹"权限不足"→ 引导升级会员
   返回 code=1  → 放行，并每 60 秒发一次心跳上报使用时长
```

**因此：直接从书签打开平台网址会看到"需要登录"，这是正常的。** 正确入口是走门户：

```
www.zstats.cn  →  顶部 ZstatsAdv 下拉  →  INSPIRE围手术期数据库
              →  选"数据提取 N"  →  自动带票据跳到 zs.medsta.cn（已登录）
```

### 1.4 四个访问入口

实测门户下拉菜单 `数据提取 1~4` 指向的地址：

| 入口 | 地址 | 说明 |
|---|---|---|
| 数据提取 1 | `https://zs.medsta.cn/Inspire_encryption/Inspire_data/inspire_data_1.0.2/` | 主入口 |
| 数据提取 2 | `https://zs.medsta.cn/Inspire_encryption/Inspire_data/inspire_data_1.0.2_a/` | 同一份数据的备用实例（变量数同为 9,793） |
| 数据提取 3 | `http://39.170.49.93:3838/Inspire_encryption/Inspire_data/Inspire_data_1.0.2` | 备用服务器 |
| 数据提取 4 | `http://39.170.49.93:3838/Inspire_encryption/Inspire_data/Inspire_data_1.0.2_a` | 备用服务器备用实例 |

> 门户同一层级还有"INSPIRE 数据整理系统 / 数据分析系统 / 数据绘图平台"，属于下游环节，本指南不涉及。

### 1.5 页面结构（五步工作流）

| 步骤 | 面包屑 | 干什么 |
|---|---|---|
| 1 | 数据查询 | 搜索变量、勾选进购物车 |
| 2 | 变量整合及队列设置 | 把变量分配成暴露 / 协变量 / 结局，并设定时间窗与提取方式 |
| 3 | 数据整理 | 衍生新变量、定量转分类、看缺失、看数据结构 |
| 4 | 纳排标准设置 | 排除人群、画流程图 |
| 5 | （步骤 4 下半屏） | 下载数据 |

### 1.6 容量与限制

| 限制 | 数值 |
|---|---|
| 变量购物车上限 | **20 个** |
| 变量列表每页 | 10 行（共 980 页） |
| 最终数据集行数 | 130,946（= 全部纳入的手术台数） |
| 平台内置建议 | 每次搜索产生 3–5 个变量就立刻整合，不要攒十几个再整合 |

---

## 第二章 INSPIRE 数据的核心设计：分阶段

### 2.1 为什么必须分阶段

围手术期研究的核心难点是：**同一个指标，在一次住院里会被测很多次，而"哪一次"才是你要的？**

举例，一个患者一次住院：

```
入院 ──┬── 术前 3 天 白蛋白 32
       ├── 术前 1 天 白蛋白 31
       ├── 手术开始
       ├── 术后 2 小时 白蛋白 28
       ├── 术后 1 天 白蛋白 30
       └── 出院
```

如果只说"取白蛋白"，程序不知道你要哪一个。INSPIRE 的解决方案是：**先把时间轴切成固定的几个阶段，再在每个阶段内部取数。**

平台的做法更彻底——它把"变量 × 阶段"直接展开成变量字典里的一行：

```
实验室检查表 | 入院前       | albumin | Albumin | 白蛋白
实验室检查表 | 入院到手术开始 | albumin | Albumin | 白蛋白
实验室检查表 | 手术期间      | albumin | Albumin | 白蛋白
实验室检查表 | 手术结束到出院 | albumin | Albumin | 白蛋白
```

**你在平台里看到的"一个变量"，其实是"某表某个变量在某个时间阶段的取值"。**

### 2.2 五个阶段（全人群版）

平台"请选择暴露的阶段"共 5 个选项，"变量所在阶段"筛选器再加上一个"基线特征"，共 6 个标签：

| 阶段（平台中文） | 英文标签 | 起止边界 | 典型用途 |
|---|---|---|---|
| 基线特征 | — | 手术当次的一条 | 人口学、手术类型（age/sex/asa/…） |
| 入院前 | `pre_admission` | 入院时刻之前 | 术前合并症、院前用药、院前检验 |
| 入院到手术开始 | `admission_to_opstart` | 入院 → 入手术室/切皮 | 术前最后一次检验、术前用药 |
| 手术期间 | `opstart_to_opend` | 手术开始 → 手术结束 | 术中生命体征、麻醉用药、输液输血 |
| 手术结束到出院 | `opend_to_discharge` | 手术结束 → 出院 | 术后并发症、术后用药、转归 |
| 出院后 | `post_discharge` | 出院之后 | 远期随访、全因死亡、再入院 |

> 统计口径：变量字典中"手术期间"的条目最多（2,881 条），其次是"手术结束到出院"（1,861 条）；"基线特征"只有 8 条（就是基线表那 8 个变量）。

### 2.3 ICU 版的 7 阶段

平台首页挂的是**全人群版（5 阶段）**。但 INSPIRE 的 ICU 子队列使用 **7 阶段**，多出的两段是把"手术结束到出院"进一步切开：

```
入院前 → 入院到手术开始 → 手术期间
      → 手术结束到ICU入科 (opend_to_icuin)
      → ICU期间 (postop_icu)              ← 新增
      → ICU出科到出院 (icuout_to_discharge) ← 新增
      → 出院后
```

判定 ICU 人群靠基线表的现成列：`has_postop_icu`（1 = 术后入 ICU）、`icu_overlap_type`、`is_first_icu`，以及 `icuin_time` / `icuout_time`。
实测：全人群 130,946 台中 `has_postop_icu=1` 的为 **14,157 台**，对应 **12,356 名患者**。

### 2.4 分阶段带来的三个直接后果

1. **变量数是乘出来的**：一个变量最多出现 5 次（每阶段一次），1,138 个 ICD-10-CM 诊断码 × 5 阶段 = 4,032 条诊断类条目。这就是 9,793 这个数字的来源。
2. **"术前合并症"要用两段并集**：入院前 + 入院到手术开始，两段合起来才是完整的术前状态。
3. **阶段顺序即因果顺序**：暴露只能取"暴露阶段及之前的阶段"（协变量），结局可以取任意阶段（含出院后）。平台在界面上会明确提示这一约束（见 4.3）。

---

## 第三章 时间窗：同一变量的多次测量该取哪一次

### 3.1 时间轴

- 所有以 `_time` 结尾的列都是**分钟**，取值是相对时间轴的偏移。
- 官方定义：每位患者在数据集内的**首次入院手术时刻**被设为 time zero（原话："The time of the first admission surgery for each subject during the study period was set as time zero"），所有时间点都转成相对 time zero 的分钟数。
- 实测补充：基线表列 `admission_time` 中，**每位患者的手术记录里最早那条 100% 为 0**（130,960 条手术中，患者首条记录的 `admission_time` 全为 0）；同一患者的再入院为更大的正数（如 4,320、10,080、2,126,880）。
  → 使用时可以把每位患者的首次入院当作 0 点读，跨次入院的顺序关系仍然成立。
- 数据分辨率：生命体征与化验结果在原始数据中已聚合成**中位数，最大分辨率 5 分钟**；`chart_time` 就是 5 分钟的网格点。

### 3.2 时间窗的定义

平台每个规则组都提供"设置时间窗"开关（是/否）与一组参数。实测界面文案为：

```
设置规则组 1 的时间窗            是 / 否
规则组 1 允许变量发生在哪个阶段    以 阶段终点 为锚点，向前取 t 分钟
请输入时间 t，单位为分钟
规则组 1 的提取方式              首次 / 末次 / 均值 / 中位数 / 最大值 / 最小值
```

展开成数学式：

```
候选记录 = { 该变量在该阶段内、且 chart_time ∈ [阶段终点 − t, 阶段终点] 的记录 }
返回值   = 对候选记录按"提取方式"归约得到的一个数
```

界面底部"对变量进行处理"的汇总表会把内部参数原样列出，可以直接核对：

| 表单 | 时间范围标签 | 变量名 | method | 起始时间 | 终点时间 | 跨阶段 | 变量类型 |
|---|---|---|---|---|---|---|---|
| 实验室检查表 | 入院前 | albumin | 末次 | `segment_end - 1440` | `segment_end` | 不跨阶段 | 定量暴露 |
| 诊断表 | 手术结束到出院 | death in hospital | 首次 | `segment_end - 10080` | `segment_end` | 不跨阶段 | 分类结局 |

- `segment_start` / `segment_end` 是该阶段的边界（同为分钟）；
- `t = 1440` 即"阶段终点前 24 小时"；`t = 10080` 即"前 7 天"。

**所以"术前白蛋白"在平台里的完整定义是**：`实验室检查表 × 入院到手术开始 × albumin × 末次 × t=1440`，即"手术开始前 24 小时内、该阶段中最后一次白蛋白"。

### 3.3 六种提取方式怎么选

| 提取方式 | 含义 | 适用场景 |
|---|---|---|
| 首次 | 时间窗内最早一次 | 结局事件（首次发生）、基线状态 |
| 末次 | 时间窗内最晚一次 | **术前指标最常用**（离手术最近，最能反映术前状态） |
| 均值 | 窗内所有值的平均 | 术中血流动力学（平均动脉压均值） |
| 中位数 | 窗内所有值的中位数 | 波动大、有极端值的指标 |
| 最大值 | 窗内最大值 | 术中最高体温、峰值血糖、最低血压（用最大值/最小值） |
| 最小值 | 窗内最小值 | 术中最低 SpO₂、最低血红蛋白 |

分类变量（诊断、药物、仪器使用）的提取方式是**首次 / 末次 / 任意一次**——"任意一次"等价于"这个阶段内出现过即记 1"。

### 3.4 跨阶段时间窗

协变量与结局的规则组额外有一个开关：

```
规则组 1 允许变量跨阶段    是（跨阶段的时间窗） / 否（本阶段的时间窗）
```

- **否**：只在本阶段内部找记录（默认，语义最干净）。
- **是**：允许跨越相邻阶段找记录，用于"事件发生阶段不确定"的结局（例如术后死亡到底记在手术期间还是手术结束到出院）。

### 3.5 随访起点（结局专用）

结局面板顶部有：

```
选择随访起点
  ● 以暴露时间作为起始点（多暴露以末次暴露时间为起点）   [study_start = last_time]
  ○ 以特定时间点作为起始点（以暴露所在阶段的终点为最早可选起点） [study_start = spe_time]
```

这决定了生存分析里"从哪一刻开始算随访"。选择"以暴露时间为起点"时，平台会额外输出一列 `cohort_start_time`（实测其值等于暴露变量的 `_time`）。做 Cox 回归时，`随访时间 = 结局_time − cohort_start_time`，或者直接用平台算好的 `_follow_time` 列。

### 3.6 时间窗设置的常见错误

| 错误写法 | 后果 |
|---|---|
| t 留空或填 0 | 界面报"请设置一个大于 0 的时间"，或窗口为空导致大量缺失 |
| t 填得太大（如 10080 = 7 天）用于术前指标 | 窗口跨过了整个术前住院期，把入院时的化验也带进来了 |
| 术前指标用"首次" | 取到的是整个住院期间第一次，常常是入院时值，不是术前值 |
| 术中血压用"末次" | 只反映关腹前的一瞬间，不是手术全程 |
| 结局用"均值" | 分类结局不支持均值；连续结局用均值会掩盖峰值 |

---

## 第四章 变量字典与队列设计规则

### 4.1 变量字典 = 表单 × 阶段 × 变量

变量列表有 5 列：**表单 / 时间阶段标签 / 变量名 / 描述英文 / 描述中文**。全人群版共 9,793 行。

| 表单 | 条目数 | 说明 |
|---|---|---|
| 诊断表 | 4,032 | ICD-10-CM 前 3 位码（1,138 个码 × 各阶段） |
| 药物表 | 2,974 | 877 种通用名 + 给药途径 + ATC |
| 手术表 | 2,288 | ICD-10-PCS 手术操作码 |
| 术中生命体征表 | 256 | 73 项术中参数 × 阶段 |
| 实验室检查表 | 190 | 38 项检验 × 阶段 |
| 病房生命体征表 | 45 | 16 项病房/ICU 参数 × 阶段 |
| 基线表 | 8 | 基线特征（不参与搜索筛选） |

### 4.2 基线表是强制的

平台明确写着：

> **基线表变量必选，即使不选，系统默认全部提取**

实测：最终数据集前 33 列永远是基线表全部列（27 个原始时间/人口学列 + 6 个衍生结局列），无论你在购物车里选了什么。

这 6 个衍生结局列可以直接当结局变量用，不必自己去诊断表里捞：

| 列名 | 含义 |
|---|---|
| `is_inhosp_death` | 院内死亡（0/1） |
| `is_first_admission` | 是否首次入院 |
| `is_first_surgery` | 是否首次手术 |
| `is_first_icu` | 是否首次入 ICU |
| `icu_overlap_type` | ICU 重合类型（0=不入 ICU / 1=术后常规入 ICU / 2=术后非预期 / …） |
| `has_postop_icu` | 是否术后入 ICU（1 = 是） |

再加上时间列 `inhosp_death_time`、`allcause_death_time`、`icuin_time`、`icuout_time`、`admission_time`、`discharge_time`，足以支撑大部分简单结局定义。

### 4.3 暴露 / 协变量 / 结局 的阶段约束

这是平台最重要的设计规则，界面会随你选定的暴露阶段实时提示：

| 角色 | 可选阶段 | 平台原文 |
|---|---|---|
| 暴露 | 你在第一步选定的那**一个**阶段 | "当前暴露变量选择阶段为：入院前 / 暴露变量仅从"入院前"阶段中选择。" |
| 协变量 | 暴露阶段**及其之前**的阶段 | "当前协变量可选择的阶段为(暴露及之前的阶段)：" |
| 结局 | **任意阶段**（含出院后） | "当前结局变量可选择的阶段为：入院前 / 入院到手术开始 / 手术期间 / 手术结束到出院 / 出院后" |

**因此第一步的"请选择暴露的阶段"单选不是筛选器，而是一个声明**——它决定第二步里哪些变量有资格当暴露。实测：切换该单选不会改变变量列表的行数（始终 9,793 行），只影响第二步的可选项。

结局面板还有一句补充：

> 此外，还可以选择当前暴露阶段"入院前"中未被暴露变量和协变量使用的变量。

即：暴露阶段里"没用完"的变量，可以再拿去当结局，但不能既当暴露又当结局。

### 4.4 变量可得率

不是每台手术都有每类记录，做样本量估算时必须先查可得率。实测各表覆盖的手术台数（分母 130,946）：

| 表 | 覆盖手术台数 | 覆盖率 |
|---|---|---|
| 药物表 | 130,796 | 99.9% |
| 术中生命体征表 | 130,915 | 99.98% |
| 病房生命体征表 | 129,315 | 98.8% |
| 实验室检查表 | 128,775 | 98.3% |
| 诊断表 | 125,971 | 96.2% |
| 手术表 | 130,946 | 100% |

平台的"数据库变量缺失预览"和纳排面板的"变量编码信息"可以直接看到每个变量的缺失个数。

---

## 第五章 功能逐项操作说明

### 5.0 登录

1. 打开 <https://www.zstats.cn/>，点右上角"个人中心"或登录入口；
2. 用**微信扫码并关注公众号**即完成登录；
3. 顶部导航 `ZstatsAdv` → `INSPIRE围手术期数据库` → 进入工具列表页；
4. 选 `数据提取 1`（或 2/3/4 备用），页面会在新标签页打开平台并自动带票据登录。

若直接打开平台地址，会看到一个居中的"需要登录"弹窗（按钮：**去登录** / **返回首页**），点"去登录"会跳到门户登录页。

### 5.1 第一步 数据查询

**页面组成**：左侧"使用指南"信息卡 → "数据库变量筛选"表单 → "请选择暴露的阶段" → "INSPIRE变量列表" → "变量购物车"。

#### 5.1.1 变量描述搜索（唯一需要回车的地方）

- 输入框 placeholder：*"请输入文字后使用Enter回车键完成筛选"*；
- 支持布尔运算：`|` 表示"或"，`&` 表示"与"。例如 `性别|体重`、`白蛋白&术前`；
- **必须按回车**才会触发筛选（回车会触发页面上一个隐藏按钮 `#nschoose_page-enter`）。
- 输入框为空 = 不过滤，显示全部 9,793 条。

实测：搜索 `性别|体重` 返回 2 条（sex、weight）；搜索 `白蛋白` 返回 10 条（含 `albumin` × 4 阶段、`alb5/alb20` 输液量 × 3 阶段）；搜索 `年龄` 返回 8 条（含 `age`、`height`、`H25 年龄相关性白内障`、`R54 与年龄相关的身体虚弱` 等多个阶段副本）。
→ **搜索匹配的是中文描述与英文描述的字符串，不只是变量名**，所以会搜出诊断码。

#### 5.1.2 四个筛选器

| 筛选器 | 可选项 |
|---|---|
| 表单 | 病房生命体征表 / 基线表 / 实验室检查表 / 手术表 / 术中生命体征表 / 药物表 / 诊断表 |
| 变量所在阶段 | 出院后 / 基线特征 / 入院到手术开始 / 入院前 / 手术结束到出院 / 手术期间 |
| 变量名 | 变量名字典（可输入搜索） |
| 描述 | 描述字典（可输入搜索） |

均为可搜索下拉（virtual-select），支持多选。右侧"清理选择 / Reset filters"按钮可一键清空筛选。

#### 5.1.3 请选择暴露的阶段

5 个单选：入院前 / 入院到手术开始 / 手术期间 / 手术结束到出院 / 出院后，默认"入院前"。
**作用**：声明本次研究的暴露阶段，决定第二步"暴露的处理"里能够选择的变量范围。它**不**过滤本页的变量列表。

#### 5.1.4 变量列表

- 每行：复选框 / 表单 / 时间阶段标签 / 变量名 / 描述英文 / 描述中文；
- 表头可排序，底部翻页（实测 980 页 × 10 行）；
- 左上"清理选择"清空本页勾选；
- 表头复选框可全选当前页。

#### 5.1.5 变量购物车

- 标题即限制：*"变量购物车（购物车总数量不能超过 20 个，后续请整合购物车变量后，再行添加）"*；
- 提示：*""购物车"的变量可能不是您想要的，请您仔细检查，对不需要的变量去掉 ☑"* → 取消勾选即可移除；
- 按钮：**清空购物车** / **下载变量信息** / **选择变量信息文件**（上传历史购物车）；
- 列表列：表格 / 阶段 / 变量名 / 描述中文。

#### 5.1.6 本页操作清单

```
[ ] 想清楚研究问题，确定暴露阶段（决定"请选择暴露的阶段"单选）
[ ] 用中文关键词搜索暴露变量 → 勾选（只勾目标阶段那一行）
[ ] 用中文关键词搜索结局变量 → 勾选
[ ] 检查购物车，取消误勾的变量（尤其注意同名变量的不同阶段副本）
[ ] 变量数 ≤ 20；建议每搜到 3–5 个就先整合一次
[ ] 点击"变量整合和队列设计"进入第二步
```

---

### 5.2 第二步 变量整合及队列设计

**页面组成**：本阶段已选变量 → 对变量进行处理（设置文件的保存/上传）→ 暴露的处理 / 协变量的处理 / 结局的处理 三个选项卡 → 🚀 载入最终数据 → 进入数据整理。

#### 5.2.1 本阶段已选变量

表格列出购物车内容，列为：表单 / 时间阶段标签 / 变量名 / 描述英文 / 描述中文 / `times`。
按钮：

- **重新载入最近历史变量信息**：一键恢复上一次整合的变量清单；
- **下载变量信息**：把当前清单存成文件，便于下次直接上传；
- **选择变量信息文件**（Browse…）：上传之前下载的清单。

#### 5.2.2 设置文件的保存与上传（全局功能）

"对变量进行处理"区块：

| 按钮 | 作用 |
|---|---|
| 保存变量处理的设置 | 下载当前全部规则组配置（时间窗、提取方式等） |
| 上传变量处理的设置 | 上传配置文件，一键恢复配置 |
| 选择变量提取设置文件（Browse…） | 上传入口 |
| 下载变量提取设置 | 下载当前配置 |

> 这套"下载/上传"是平台唯一的**可复现性保障**：论文方法学部分要写清楚提取参数，也必须靠这个文件留档。

#### 5.2.3 暴露的处理

顶部约束条：

```
当前暴露变量选择阶段为：入院前
暴露变量仅从"入院前"阶段中选择。
```

两类规则组，可分别"增加一组 / 减少一组"：

| 类型 | 按钮 ID 语义 | 提取方式选项 |
|---|---|---|
| **分类暴露** | 增加一组对分类暴露变量的设置 | 首次 / 末次 / 任意一次 |
| **定量暴露** | 增加一组对定量暴露变量的设置 | 首次 / 末次 / 均值 / 中位数 / 最大值 / 最小值 |

每个"处理第 N 组…"面板包含：

```
选择规则组 N 的变量        ← 多选，只列出该阶段内符合条件的变量
设置规则组 N 的时间窗       是 / 否
规则组 N 允许变量发生在哪个阶段   以 阶段终点 为锚点，向前取 t 分钟  [segment_end]
请输入时间 t，单位为分钟     ← 数字框，必须 > 0
规则组 N 的提取方式        首次 / 末次 / 均值 / 中位数 / 最大值 / 最小值
```

**多组的意义**：一个研究可以有多个暴露（如"术前白蛋白"+"术中最低血压"），分成多组分别设置时间窗。

#### 5.2.4 协变量的处理

顶部约束条：

```
当前协变量可选择的阶段为(暴露及之前的阶段)：入院前
```

同样分 **分类协变量 / 定量协变量** 两类规则组，每个规则组比暴露多一个开关：

```
设置规则组 N 的时间窗        是 / 否
规则组 N 允许变量跨阶段      是（跨阶段的时间窗） / 否（本阶段的时间窗）
规则组 N 允许变量发生在哪个阶段   以 阶段终点 为锚点，向前取 t 分钟
请输入时间 t，单位为分钟
规则组 N 的提取方式          首次 / 末次 / 任意一次（分类）
```

#### 5.2.5 结局的处理

顶部内容：

```
当前结局变量可选择的阶段为：
  入院前 / 入院到手术开始 / 手术期间 / 手术结束到出院 / 出院后
此外，还可以选择当前暴露阶段"入院前"中未被暴露变量和协变量使用的变量。

选择随访起点
  ● 以暴露时间作为起始点（多暴露以末次暴露时间为起点）
  ○ 以特定时间点作为起始点（以暴露所在阶段的终点为最早可选起点）
```

规则组分两类：

| 类型 | 提取方式选项 | 特有参数 |
|---|---|---|
| **分类结局** | 首次 / 末次 / 任意一次 | 允许变量跨阶段、随访时间 t |
| **定量结局** | 首次 / 末次 / 均值 / 中位数 / 最大值 / 最小值 | 同上 |

#### 5.2.6 🚀 载入最终数据

点击后平台会在服务端把长表按规则组归约成宽表，加载完成后：

- 弹提示：**更新完成，您可继续前往第一页挑选指标**；
- 页面下方出现数据预览表，标题栏第一行就是最终列名。

实测（130,946 行 × 39 列）末尾新增的衍生列：

```
实验室检查表_入院前_albumin_末次
实验室检查表_入院前_albumin_末次_time
cohort_start_time
诊断表_手术结束到出院_death in hospital_首次
诊断表_手术结束到出院_death in hospital_首次_time
诊断表_手术结束到出院_death in hospital_首次_follow_time
```

> **重要操作顺序**：每加一批变量都要"载入最终数据"一次，否则新增的规则组不会进入数据集。可以反复回到第一步加变量，再加回来重新载入。

---

### 5.3 第三步 数据整理

**页面组成**：打开我的数据 → 数据初步整理（衍生新变量 / 定量变量转分类）→ 整理后数据（数据集 / 数据库变量概览 / 数据库变量缺失预览）。

#### 5.3.1 衍生新变量

| 控件 | 说明 |
|---|---|
| 新增变量名 | 自己命名，会成为新的一列 |
| 计算方法的选择 | ○ 简单求和　○ 自定义 |
| 请选择连续型变量 | 参与计算的一列或多列 |
| 将涉及计算的变量缺失转为 0（不改变原数据） | 复选框，建议勾上，否则任何一列缺失结果就是缺失 |
| 确认 | 生成新变量 |

典型用途：把多个血压记录求和、构造合并症计数等。

#### 5.3.2 定量变量转分类

| 控件 | 说明 |
|---|---|
| 请选择连续型变量 | 要分组的列（如白蛋白） |
| 请选择分组方式 | 自定义分组 / 按照中位数分组 / 三分位数 / 四分位数 / 五分位数 / **转换为标准化 z 值** |
| 请输入分组界值（请以英文的逗号 , 做分隔） | 仅"自定义分组"时需要，如 `30,35` |
| 开始分组 | 生成分组列 |

> 用"按照中位数分组"一键做出高/低两组，是围手术期研究里最常见的处理方式。

#### 5.3.3 整理后数据三个页签

| 页签 | 内容 |
|---|---|
| 数据集 | 完整数据表格（DataTables），可搜索、可分页 |
| 数据库变量概览 | R 的 `str()` 输出，逐列显示类型与前几个取值。实测显示：`Classes 'data.table' and 'data.frame': 130946 obs. of 39 variables` |
| 数据库变量缺失预览 | 各列缺失情况 |

---

### 5.4 第四步 数据纳排与流程图

点击 **进入数据纳排** 进入。左上按钮 **打开我的数据** 见 5.5。

#### 5.4.1 页面组成

```
原始数据集(待纳排)          ← 预览，仍是 Excel 出图前的宽表
变量编码信息                ← 查看某变量的取值分布与编码范围
保存纳排信息 / 上传纳排信息
[模块1] 剔除不符条件的研究对象
[模块2] 剔除暴露、结局变量值不符合要求的研究对象
[模块3] 剔除其他变量/协变量值不符合要求的研究对象
纳排流程图
纳排后数据集（Excel / SPSS / 下载纳排后数据集）
```

#### 5.4.2 变量编码信息

选择变量后显示（实测选 `case_id`）：

```
变量编码信息
case_id
缺失个例: n = 109851
非缺失个例: n = 21095
编码信息:
    -32763 to 32767: Range of Values
```

**用途**：写排除条件之前，先看看这个变量到底有哪些取值、缺失多少。分类变量会列出取值 + 频数。

配套下拉：

- `变量编码信息`（选择要看哪个变量）；
- `编码值`（选择要展开的具体取值）。

#### 5.4.3 三个排除模块

三个模块结构相同，只是作用对象不同：

| 模块 | 面板标题 | 管什么 |
|---|---|---|
| 1 | 请以排除的方式，剔除不符条件的研究对象 | 人群特征（年龄、性别、科室、是否急诊…） |
| 2 | 请以排除的方式，剔除暴露、结局的变量值不符合要求的研究对象 | 暴露缺失/异常、结局缺失/异常 |
| 3 | 请以排除的方式，剔除其他变量的变量值不符合要求的研究对象 | 协变量缺失/异常 |

每个模块内：

```
说明: 我们开展数据分析的时候，很多时候往往会筛选子集进行分析，例如女性，老年人，高血压患者等，
     或者排除不符合条件的对象。
[添加纳排条件] [清除所有条件]
☑ 是否自动剔除缺失
示例1: 缺失值排除请使用 = 搭配 NA;
示例2: 范围值排除，比如排除18-30的值请使用 区间(n1-n2) 搭配 18-30;
```

点"添加纳排条件"后新增一行条件，每行 5 个控件：

```
+排除条件   [EXCLUDE]  变量 [case_id ▾]  运算 [= ▾]  变量值 [____]  [删除条件]
```

- 逻辑固定为 `EXCLUDE`（默认口径就是"排除掉满足条件的人"）；
- 变量下拉支持输入搜索（实测选择器内部为 selectize，变量选项较多）；
- 运算 + 取值的写法见下表。

#### 5.4.4 排除条件写法

| 目的 | 运算 | 取值 |
|---|---|---|
| 排除缺失 | `=` | `NA` |
| 排除某个取值 | `=` | 取值本身（如 `1`） |
| 排除区间内的值 | `区间(n1-n2)` | `18-30` |
| 排除大于某值 | `>` | 数值 |
| 排除小于某值 | `<` | 数值 |

#### 5.4.5 是否自动剔除缺失

默认勾选。作用是：**只要你添加了任何条件，就自动把该变量缺失的个体一起剔掉**。
如果你的研究允许缺失（例如做多重插补），应当手动取消勾选，否则样本量会被无谓削减。

#### 5.4.6 纳排流程图

"纳排流程图"面板内点 **创建/更新流程图**，平台用 `viz.js` 画出标准的入组流程图（初始人数 → 各排除步骤 → 最终人数）。流程图的节点来自你在三个模块里设置的条件。

> 下载数据前有硬性提示：**请创建/更新流程图后再下载数据**。流程图既是论文 Figure 1 的现成素材，也是纳排逻辑的留档。

#### 5.4.7 保存 / 上传纳排信息

- **保存纳排信息**：把三个模块的条件 + 流程图配置存成文件；
- **上传纳排信息**：上传恢复。平台提示：*"上传后，请务必点开各纳排模块确认纳排信息"*。

#### 5.4.8 下载

| 按钮 | 产物 |
|---|---|
| Excel | 下载为 .xlsx |
| SPSS | 下载为 SPSS 格式 |
| 下载纳排后数据集 | 打包下载完整数据 |
| 下载数据 / 下载纳排信息 | 数据与纳排配置分别下载 |

实测点击"下载纳排后数据集"会弹出遮罩提示 **"正在打包数据，请耐心等待！"，** 因为是全量 130,946 行 × 数十列，需要等待。

---

### 5.5 打开我的数据（导入外部数据）

第二、三、四步都有一个 **打开我的数据** 按钮，打开的是 `shinydashboard` 的 **Import data** 弹窗：

| 页签 | 内容 |
|---|---|
| Import | 上传文件 → 选择文件（支持 `.csv / .txt / .xls / .xlsx / .rds / .fst / .sas7bdat / .sav`）→ 设置读取参数 → Import |
| View | 预览已导入数据 |
| Update | 更新数据 |

读取参数：

| 参数 | 说明 |
|---|---|
| Rows to skip before reading data | 跳过表头前的行数 |
| Missing values character(s) | 缺失值标记，默认 `NA`，多个用逗号分隔 |
| Decimal separator | 小数点符号 |
| Encoding | 文件编码（**导出的中文 CSV 常需选 UTF-8 或 GBK**） |

**用途**：把你在别处整理好的数据集（或历史提取结果）导入平台，继续做纳排、画流程图、下载 Excel/SPSS。平台内置说明第 4 条就是讲这个：

> 我们平台支持导入历史数据集，请在第二页数据集整合界面进行操作

---

## 第六章 完整提取流程示例（含实测结果）

### 6.1 研究问题

> 术前白蛋白水平与术后院内死亡的关系（全人群）。

### 6.2 变量设计

| 角色 | 变量 | 表单 | 阶段 | 时间窗 | 提取方式 |
|---|---|---|---|---|---|
| 暴露 | albumin | 实验室检查表 | 入院前 | t = 1440 分钟 | 末次 |
| 结局 | death in hospital | 诊断表 | 手术结束到出院 | 随访 t = 10080 分钟 | 首次 |
| 协变量 | age / sex / asa / emop / department / antype | 基线表 | 基线特征 | — | 全部自动带入 |

### 6.3 逐步操作

```
① 门户登录 → INSPIRE围手术期数据库 → 数据提取 1
② 第一步：不要改"请选择暴露的阶段"（默认入院前）
   搜索"白蛋白" → 勾选 实验室检查表 | 入院前 | albumin
   搜索"院内死亡" → 勾选 诊断表 | 手术结束到出院 | death in hospital
   购物车应有 2 行 → 点"变量整合和队列设计"
③ 第二步 · 暴露的处理 → 增加一组对定量暴露变量的设置
   选择变量 = albumin (白蛋白)
   时间窗 = 是；t = 1440；提取方式 = 末次
④ 第二步 · 结局的处理 → 增加一组对分类结局的设置
   选择变量 = death in hospital (院内死亡)
   时间窗 = 是；允许变量跨阶段 = 否（本阶段的时间窗）；随访时间 t = 10080；提取方式 = 首次
   选择随访起点 = 以暴露时间作为起始点（默认）
⑤ 点 🚀 载入最终数据 → 等提示"更新完成…"
⑥ 点 进入数据整理 → 按需做衍生变量 / 定量转分类
⑦ 点 进入数据纳排 → 三个模块里加排除条件 → 创建/更新流程图
⑧ 点 Excel / SPSS / 下载纳排后数据集
```

### 6.4 实测结果

**最终数据集规模**：130,946 行（每行一台手术），39 列。

**列结构**：

```
[1-27]  基线表原始列
        subject_id, hadm_id, op_id, case_id, opdate, age, sex, weight, height, asa,
        emop, department, antype, orin_time, orout_time, opstart_time, opend_time,
        admission_time, discharge_time, anstart_time, anend_time, cpbon_time, cpboff_time,
        icuin_time, icuout_time, inhosp_death_time, allcause_death_time
[28-33] 基线表衍生结局列
        is_inhosp_death, is_first_admission, is_first_surgery, is_first_icu,
        icu_overlap_type, has_postop_icu
[34-40] 你选的变量（暴露在前、结局在后）
        实验室检查表_入院前_albumin_末次
        实验室检查表_入院前_albumin_末次_time
        cohort_start_time
        诊断表_手术结束到出院_death in hospital_首次
        诊断表_手术结束到出院_death in hospital_首次_time
        诊断表_手术结束到出院_death in hospital_首次_follow_time
```

（上表按 39 列计，实际列数随所选变量增减。）

**数据样式（节选前 3 台手术）**：

| op_id | age | sex | asa | emop | 白蛋白_末次 | 白蛋白_time | cohort_start_time | 院内死亡 | 死亡_time | follow_time |
|---|---|---|---|---|---|---|---|---|---|---|
| 435838278 | 60 | 0 | 2 | 0 | （缺失） | （缺失） | （缺失） | 0 | — | 0 |
| 456749370 | 55 | 0 | 1 | 0 | 4.4 | −225 | −225 | 0 | — | 0 |
| 444859284 | 75 | 1 | 2 | 0 | （缺失） | （缺失） | （缺失） | 0 | — | 0 |

可以读出三件事：

1. **暴露变量有 `_time` 陪伴列**，记录了该值发生在时间轴的哪个位置（−225 = 入院前 225 分钟）；
2. **结局变量**有 `_time` + `_follow_time`，分别记录事件发生时间与随访时长；
3. `cohort_start_time` 等于暴露时间（本例 −225），这就是生存分析的随访起点。

### 6.5 论文方法学段落模板

> 本研究数据来自 INSPIRE（Informative Surgical Patient dataset for Innovative Research Environment）围手术期公开数据库（韩国首尔国立大学医院，2011 年 1 月至 2020 年 12 月，约 13 万例手术）。数据通过 ZSTATS 平台的 INSPIRE 数据提取系统（inspire_data_1.0.2）提取。暴露定义为**入院前 24 小时内最后一次血清白蛋白**（提取参数：阶段=入院前，时间窗=阶段终点前 1440 分钟，提取方式=末次）；主要结局为**术后院内死亡**（提取参数：阶段=手术结束到出院，时间窗=阶段终点前 10080 分钟，提取方式=首次）。随访起点为暴露测量时间。共纳入 130,946 台手术。

### 6.6 离线分析（R）

```r
library(data.table)

d <- fread("inspire_export.csv", encoding = "UTF-8")

setnames(d,
  c("实验室检查表_入院前_albumin_末次", "诊断表_手术结束到出院_death in hospital_首次"),
  c("albumin", "inhosp_death")
)

# 暴露分组：按中位数
d[, alb_grp := ifelse(albumin > median(albumin, na.rm = TRUE), "High", "Low")]

# 逻辑回归
fit <- glm(inhosp_death ~ alb_grp + age + sex + asa + emop,
           data = d, family = binomial)
summary(fit)
exp(cbind(OR = coef(fit), confint(fit)))

# 生存分析（用平台给的随访时间，单位分钟 → 天）
d[, follow_days := `诊断表_手术结束到出院_death in hospital_首次_follow_time` / 1440]
library(survival)
survfit(Surv(follow_days, inhosp_death) ~ alb_grp, data = d)
```

---

## 第七章 注意事项与避坑清单

### 7.1 平台使用层面

| 症状 | 原因 | 解决 |
|---|---|---|
| 打开平台网址弹"需要登录" | 跨域工具站的 token 只能由门户签发 | 从 www.zstats.cn 门户进入 |
| 搜索框敲了字但列表没变 | 回车没有真正触发隐藏的 Trigger 按钮 | 确认输入框获得焦点后按回车；或按 Tab 让输入框失焦后回车 |
| 搜索结果只有一行空白 | 关键词在字典里确实没有匹配 | 换关键词，或改用变量名（英文）搜索 |
| 点"添加纳排条件"没反应 | 条件行渲染在折叠面板里，Shiny 对隐藏输出延迟渲染 | 先展开对应面板，再点按钮 |
| 变量下拉只有一项 | 下拉是服务端搜索型 selectize，选项按输入动态加载 | 在框里输入关键词再选 |
| 载入最终数据后没有新列 | 规则组里的变量是"默认显示值"，未真正写入 Shiny 输入 | 在变量下拉里**重新点选一次**该变量，再点载入最终数据 |
| 纳排条件加了但人数没变 | "是否自动剔除缺失"或条件本身没生效 | 点开模块确认条件行还在，必要时重新加载纳排信息 |
| 下载按钮点了没反应 | 数据在服务端打包，需要等待 | 看到"正在打包数据，请耐心等待！"属正常，等它结束 |

### 7.2 统计设计层面

1. **同名变量有多个阶段副本**，勾选时务必看清"时间阶段标签"那一列，勾错阶段等于换了一个变量。
2. **时间窗 t 必须显式设置**，默认 0 会报错；术前指标常用 1440（24 h）或 10080（7 d），术中指标通常覆盖整个手术期间。
3. **术前合并症 = 入院前 + 入院到手术开始 两段并集**，只取一段会漏。
4. **"任意一次"和"首次"的区别**：做"术后是否发生并发症"用"任意一次"；做"首次并发症时间"用"首次"。
5. **随访起点要和研究设计一致**：用"以暴露时间为起点"时，`cohort_start_time` 就是暴露时间；如果暴露本身在术前很久，会导致左截断，需在模型里说明。
6. **样本量必须用"可得率"打折**：某变量只有 96% 的手术有记录，最终样本量上限就是 96%。
7. **不要一次选 20 个变量**：购物车上限 20，且每加一批都要重新载入数据，分批做更容易定位错误。
8. **务必保存三类设置文件**（变量信息 / 变量提取设置 / 纳排信息），这是论文可复现性的唯一凭据。
9. **流程图必须在下载前生成**，平台会强制校验。
10. **导入外部数据时留意编码**：中文 CSV 建议 UTF-8；平台缺少值标记默认 `NA`，与 R 一致。

### 7.3 合规层面

- INSPIRE 是**去标识化公开数据集**（k-匿名 129、l-多样性 58，重识别风险 < 0.002%），但 PhysioNet 仍要求资质认证 + 签署数据使用协议；
- 平台是第三方工具，**数据下载与使用须自行确认授权范围**；
- 论文中应引用官方文献：Lee H, Lim L. INSPIRE, a publicly available research dataset for perioperative medicine (version 1.0). PhysioNet. 2023. DOI: 10.13026/jyzb-ez61。

---

## 附录 A 五阶段定义速查

| 序号 | 阶段 | 英文标签 | 起 | 止 | 常用指标 |
|---|---|---|---|---|---|
| 0 | 基线特征 | — | 手术当次 | 手术当次 | age/sex/weight/height/asa/emop/department/antype |
| 1 | 入院前 | `pre_admission` | 数据起点 | 入院时刻 | 既往病史（ICD-10-CM）、院前用药 |
| 2 | 入院到手术开始 | `admission_to_opstart` | 入院时刻 | 入手术室/切皮 | 术前检验、术前用药、术前生命体征 |
| 3 | 手术期间 | `opstart_to_opend` | 切皮 | 手术结束 | 术中生命体征、麻醉药、输液输血、尿量、失血 |
| 4 | 手术结束到出院 | `opend_to_discharge` | 手术结束 | 出院 | 术后检验、术后用药、病房生命体征、并发症 |
| 5 | 出院后 | `post_discharge` | 出院 | 数据终点 | 远期并发症、全因死亡、再入院 |

ICU 版在此基础上把"手术结束到出院"拆成：`手术结束到ICU入科` → `ICU期间` → `ICU出科到出院`。

## 附录 B 输出字段命名规则

```
表单_阶段_变量名_提取方式              例如 实验室检查表_入院前_albumin_末次
…_time                                 该值在时间轴上的位置（分钟）
…_follow_time                          仅结局有：随访时长（分钟）
cohort_start_time                      随访起点（分钟）
```

固定列（无论选什么变量都出现）：

```
subject_id  hadm_id  op_id  case_id  opdate  age  sex  weight  height  asa  emop
department  antype  orin_time  orout_time  opstart_time  opend_time  admission_time
discharge_time  anstart_time  anend_time  cpbon_time  cpboff_time  icuin_time
icuout_time  inhosp_death_time  allcause_death_time
is_inhosp_death  is_first_admission  is_first_surgery  is_first_icu
icu_overlap_type  has_postop_icu
```

## 附录 C 平台内置文案原文

**第一步 左侧的使用指南**：

> 1. 变量搜索一般使用中文变量描述或者英文变量名去搜索
> 2. 该平台是数据整理与分析平台，如果你需要原始数据，请到 Inspire 官网下载：<https://physionet.org/content/inspire/1.0/>
> 3. 尽量搜索产生 3-5 个变量后，立刻点击整合、立刻更新数据集，不要等搜索了十几个变量后再去创建数据集，容易出错，而且不容易找到出错原因
> 4. 我们平台支持导入历史数据集，请在第二页数据集整合界面进行操作

**第一步 变量列表上方**：

> 注意：此处提供的是 INSPIRE 数据集变量信息，请在下方勾选需要研究的变量
> 基线表变量必选，即使不选，系统默认全部提取

**第四步 模块一说明**：

> 说明：我们开展数据分析的时候，很多时候往往会筛选子集进行分析，例如女性，老年人，高血压患者等，或者排除不符合条件的对象。
> 示例1：缺失值排除请使用 = 搭配 NA；
> 示例2：范围值排除，比如排除 18-30 的值请使用 区间(n1-n2) 搭配 18-30；

**第四步 上传纳排信息后的提示**：

> 上传后，请务必点开各纳排模块确认纳排信息

**下载前提示**：

> 提示：请创建/更新流程图后再下载数据

## 附录 D 术语表

| 术语 | 含义 |
|---|---|
| INSPIRE | Informative Surgical Patient dataset for Innovative Research Environment，韩国首尔国立大学医院围手术期公开数据集 |
| op_id | 手术唯一标识（以 4 开头），本平台的分析单元 |
| hadm_id | 住院唯一标识（以 2 开头），一次住院可含多台手术 |
| subject_id | 患者唯一标识（以 1 开头），一个患者可多次住院 |
| time zero | 时间轴零点，官方定义为每位患者的首次入院手术时刻 |
| 阶段 / stage / segment | 把时间轴切成的固定区段，见附录 A |
| 时间窗 | 阶段内"阶段终点前 t 分钟"的子区间 |
| 提取方式 | 首次 / 末次 / 均值 / 中位数 / 最大值 / 最小值（分类变量另有"任意一次"） |
| 暴露 / 协变量 / 结局 | 研究的自变量 / 混杂变量 / 因变量，各自受阶段约束 |
| 可得率 | 某变量在实际数据中有记录的手术占比 |
| 购物车 | 第一步的变量暂存区，上限 20 个 |
| 变量编码信息 | 查看某个变量的缺失数、取值与编码范围 |
| 纳排流程图 | 平台自动生成的人群筛选流程图（论文 Figure 1 素材） |
